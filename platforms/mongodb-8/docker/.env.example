# IF GNU MAKE IS INSTALLED - IT IS NOT REQUIRED TO UPDATE THIS ENVIRONMENT FILE
# REMOVE COMMENTS WHEN COPY THIS FILE AND TRIM TRAILING WHITESPACES
# Ask the team for recommended values

COMPOSE_LEAD=myproj                             # <- lead abbreviation or acronym as part of related containers naming rule -------------------------> #
COMPOSE_CNET=mp-dev                             # <- useful for networking to connect between containers --------------------------------------------> #
COMPOSE_IMGK=alpine-3.22-mongodb-8.22           # <- main container property for pulling and caching image into docker ------------------------------> #
COMPOSE_NAME=mp-mongodb-dev                     # <- container name to build the service - it is important to set the environment in this variable --> #
COMPOSE_HOST=127.0.0.1                          # <- machine hostname referrer - not necessary for this project -------------------------------------> #
COMPOSE_CPUS=2.00                               # <- container's maximum CPUs usage to apply by docker-compose - leave it empty for full usage ------> #
COMPOSE_MEMO=512M                               # <- container's maximum RAM usage to apply by docker-compose ---------------------------------------> #
COMPOSE_SWAP=1G                                 # <- container's RAM swap space in storage executed by automation command ---------------------------> #
COMPOSE_PORT=7711                               # <- local machine port opened for container service ------------------------------------------------> #
COMPOSE_DATA="./data"                           # <- platform broker data storage in local ----------------------------------------------------------> #
COMPOSE_LOGS="./logs"                           # <- platform broker logs storage in local ----------------------------------------------------------> #
MONGODB_INITDB_DATABASE=dev_local               # <- database name ----------------------------------------------------------------------------------> #
MONGODB_INITDB_ROOT_USERNAME=devuser            # <- database root user -----------------------------------------------------------------------------> #
MONGODB_INITDB_ROOT_PASSWORD="devpass"          # <- database root password -------------------------------------------------------------------------> #
COMPOSE_CLIENT_IMGK=mongo-express               # <- container property for the database client for pulling and caching image into docker -----------> #
COMPOSE_CLIENT_CPUS=1.00                        # <- client's container maximum CPUs usage to apply by docker-compose - empty for full usage --------> #
COMPOSE_CLIENT_MEMO=128M                        # <- client's container maximum RAM usage to apply by docker-compose --------------------------------> #
COMPOSE_CLIENT_SWAP=256M                        # <- client's container RAM swap space in storage executed by automation command --------------------> #
COMPOSE_CLIENT_PORT=7712                        # <- local machine port opened for client's container service client --------------------------------> #
MONGODB_EXPRESS_USERNAME=admin.user             # <- database client first user account -------------------------------------------------------------> #
MONGODB_EXPRESS_PASSWORD="admin-pass"           # <- database client first user password ------------------------------------------------------------> #
COMPOSE_IGNORE_ORPHANS=true                     # <- ignore docker warnings messages when client is in used -----------------------------------------> #